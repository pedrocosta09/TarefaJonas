spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/assistencia
    username: postgres
    password: 123456

  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.flywaydb: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE